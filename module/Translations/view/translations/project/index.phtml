<?php
$title = $this->translate('Translation projects');
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>
<p>
    <a class="btn btn-default" href="<?= $this->url('project', ['action' => 'add']) ?>"><span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span><?= $this->translate('Add new project') ?></a>
</p>

<table class="table table-striped">
<tr>
    <th>Name</th>
    <th>Git repository</th>
    <th>&nbsp;</th>
</tr>
<?php foreach ($projects as $project) : ?>
    <tr>
        <td><?= $this->escapeHtml($project->name) ?></td>
        <td><?= $this->escapeHtml($project->gitProject) ?></td>
        <td>
            <a href="<?= $this->url('project', ['action' => 'edit', 'id' => $project->id]) ?>"><span class="glyphicon glyphicon-edit" aria-hidden="true"><?= $this->translate('Edit') ?></a>
            <a href="<?= $this->url('project', ['action' => 'delete', 'id' => $project->id]) ?>"><span class="glyphicon glyphicon-minus-sign" aria-hidden="true"><?= $this->translate('Delete') ?></a>
        </td>
    </tr>
<?php endforeach; ?>
</table>